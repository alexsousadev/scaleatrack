# Nome do app (aparece na barra lateral e no titulo da aba).
# Deixe vazio e o painel mostra so o simbolo.
NEXT_PUBLIC_APP_NAME=

# Banco (Turso). Sem isso, usa ./local.db
TURSO_DATABASE_URL=
TURSO_AUTH_TOKEN=

# URL publica da app (usada nas URLs de webhook)
APP_URL=http://localhost:3210

# Senha do painel. Se ficar vazio, o painel fica aberto.
PANEL_PASSWORD=

# Segredo usado para assinar o cookie de login. Use uma string longa em producao.
PANEL_AUTH_SECRET=

# Meta Ads (Marketing API) - token de sistema com ads_read
META_ACCESS_TOKEN=
META_API_VERSION=v21.0

# Protege as rotas /api/cron/*
CRON_SECRET=troque-isto
